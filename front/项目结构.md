# 前端项目结构

## 技术栈

- **Vue 3.5**（Composition API + `<script setup>`）
- **Vue Router 4**（Hash-less History 模式）
- **Vite 7**（开发 + 构建，含 API 代理）

## 目录总览

```
front/
├── index.html                     # Vite 入口 HTML
├── package.json                   # 依赖：vue 3.5、vue-router 4、vite 7
├── vite.config.js                 # 构建配置 + /api 代理 → 127.0.0.1:8000
├── .gitignore
│
├── public/
│   └── favicon.ico
│
└── src/
    ├── main.js                    # 应用入口：createApp → use(router) → mount
    ├── App.vue                    # 顶层 Shell：导航栏 + <router-view>
    │
    ├── router/
    │   └── index.js               # 路由表：/ → ChatView, /admin → AdminView
    │
    ├── views/
    │   ├── ChatView.vue           # 聊天页（~310 行）：多模式聊天 + 知识库检索
    │   └── AdminView.vue          # 管理页（~620 行）：密码门 + 知识库管理
    │
    ├── assets/
    │   ├── base.css               # Vue 官方基础变量（日间/暗夜）
    │   ├── main.css               # 全局样式渐变背景
    │   └── logo.svg               # 默认 Logo
    │
    └── components/                # 公共组件（预留）
```

## 路由

| 路径 | 组件 | 说明 |
|---|---|---|
| `/` | ChatView | 聊天页面（仅聊天 / 智能问答 / 知识库检索） |
| `/admin` | AdminView | 管理页面（密码门 + 知识库增删改查） |

## 页面详情

### ChatView.vue — 聊天页

**三种模式：**

| 模式 | 接口 | 说明 |
|---|---|---|
| `chat_only`（仅聊天） | `POST /api/chat { mode: "chat_only" }` | 无工具无知识库，纯对话 |
| `chat`（智能问答） | `POST /api/chat { mode: "chat" }` | 支持工具调用和 RAG 检索，SSE 流式 |
| `search`（知识库检索） | `POST /api/rag/search` | 直接搜索返回相关性排序结果 |

**核心逻辑：**
- 对话模式使用 SSE 流式接收，逐 chunk 渲染
- 检索模式展示结果卡片（来源、分类、相关性分值）
- 自动拼接最近 15 条 history
- 支持 Markdown 富文本渲染（粗体、列表、段落）

### AdminView.vue — 管理页

**密码门：** 先输入管理密码（验证通过 `/api/admin/knowledge`），未通过时仅显示密码输入框。

**管理功能：**

| 功能 | 操作 | 接口 |
|---|---|---|
| 📊 结构概览 | 显示总 Chunk / 分类数 / 追踪文件数 | `POST /api/admin/knowledge` |
| 📤 上传文档 | 选 PDF/TXT + 分类 → 上传 | `POST /api/admin/upload` |
| 🗑️ 删除文档 | 点击垃圾桶 → 弹窗确认 → 删除 | `POST /api/admin/delete` |
| 📁 改分类 | 点击文件夹图标 → 弹窗选分类 → 确认 | `POST /api/admin/category` |
| 🔍 检索 Chunk | 输入关键词实时过滤，展示匹配 chunk | 客户端过滤 |
| ✏️ 编辑 Chunk | 点击任意 chunk → 弹窗编辑 → 保存 | `POST /api/admin/chunk/update` |

**交互细节：**
- 删除和改分类操作均有确认弹窗（遮罩层 + 取消/确认按钮）
- Chunk 编辑为独立宽弹窗，显示完整内容 textarea
- 分类下拉支持选择已有分类或输入自定义新分类
- 搜索有 250ms 防抖，结果以扁平列表展示，点击可编辑

## 数据流

```
用户输入
  │
  ├─ 聊天/仅聊天模式 ──→ POST /api/chat/stream (SSE)
  │                          │
  │                    逐 chunk 追加到 messages
  │                          │
  │                    最终渲染 Markdown 内容
  │
  ├─ 检索模式 ──→ POST /api/rag/search
  │                    │
  │              展示结果卡片列表
  │
  └─ 管理页 ──→ 密码验证 → 管理接口
                    │
             上传 / 删除 / 改分类 / 编辑 chunk
                    │
             自动刷新结构树
```

## 调用的接口汇总

| 接口 | 用途 |
|---|---|
| `POST /api/chat/stream` | 流式对话 |
| `POST /api/rag/search` | 知识库检索 |
| `POST /api/admin/knowledge` | 密码验证 + 获取结构 |
| `POST /api/admin/upload` | 上传文档 |
| `POST /api/admin/delete` | 删除文档 |
| `POST /api/admin/category` | 更改分类 |
| `POST /api/admin/chunk/update` | 编辑 chunk |
