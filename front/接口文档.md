# 前端接口消费文档

## 代理配置

Vite 开发服务器自动代理 `/api/*` → `http://127.0.0.1:8000`，直接写相对路径即可：

```javascript
fetch('/api/xxx')  // 自动转发到后端
```

---

## 一、对话与检索

### `POST /api/chat/stream` — SSE 流式对话（支持中断）

```javascript
const controller = new AbortController()

const response = await fetch('/api/chat/stream', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    message: '如何更换滤网',
    history: [{ role: 'user', content: '...' }, { role: 'assistant', content: '...' }],
    session_id: 'sess_abc',
    user_context: { user_id: '1004', city: '北京', month: '2026-06' },
    mode: 'chat',   // chat | chat_only
  }),
  signal: controller.signal,  // ← AbortController
})

// 用户点击停止
controller.abort()  // fetch 抛出 AbortError，前端捕获后不显示错误

// SSE 流式读取
const reader = response.body.getReader()
const decoder = new TextDecoder()
let buf = ''
while (true) {
  const { done, value } = await reader.read()
  buf += decoder.decode(value, { stream: true })
  const lines = buf.split('\n')
  buf = lines.pop() || ''
  for (const line of lines) {
    if (line.startsWith('data: ')) {
      const text = line.slice(6)
      if (text.startsWith('[ERROR]')) throw new Error(text.slice(8))
      if (text.trim()) content += text  // 逐块追加渲染
    }
  }
  if (done) break
}
```

### `POST /api/rag/search` — 知识库检索

```javascript
const resp = await fetch('/api/rag/search', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ query: 'WIFI设置' }),
})
const { data } = await resp.json()
// data.results: [{ content, score, source, category, metadata }]
```

---

## 二、管理端接口

> 先登录获取 JWT token，后续所有管理接口用 `token` 替代 `password`。token 24h 过期。

### `POST /api/admin/login` — 登录获取 JWT

```javascript
const resp = await fetch('/api/admin/login', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ token }),
})
const { data } = await resp.json()
const token = data.token  // JWT，存储为变量
```

### `POST /api/admin/knowledge` — 获取知识库完整结构

```javascript
const resp = await fetch('/api/admin/knowledge', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ token }),
})
const { data } = await resp.json()
// data: { chunk_count, category_count, tracked_file_count, collection_name, categories }
// categories: [{ name, file_count, total_chunks, files: [{ name, source, chunk_count, chunks }] }]
```

### `POST /api/admin/upload` — 上传文档

```javascript
const form = new FormData()
form.append('token', token)
form.append('category', '产品FAQ')
form.append('file', fileInput.files[0])  // PDF 或 TXT

const resp = await fetch('/api/admin/upload', { method: 'POST', body: form })
const data = await resp.json()
```

### `POST /api/admin/delete` — 删除文档

```javascript
const resp = await fetch('/api/admin/delete', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    token,
    source: 'C:\\path\\to\\file.pdf',  // 完整路径，从 knowledge 接口获取
  }),
})
```

### `POST /api/admin/category` — 更改文档分类

```javascript
const resp = await fetch('/api/admin/category', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    token,
    source: 'C:\\path\\to\\file.pdf',
    category: '新分类名',
  }),
})
```

### `POST /api/admin/chunk/update` — 编辑单个 Chunk

```javascript
const resp = await fetch('/api/admin/chunk/update', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    token,
    chunk_id: 'chunk-uuid-from-knowledge-api',
    content: '新的文本内容...',
  }),
})
```

### `POST /api/admin/prompts` — 提示词文件列表

```javascript
const resp = await fetch('/api/admin/prompts', {
  method: 'POST', headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ token }),
})
// data.prompts: [{ name, path, filename, exists, size }]
```

### `POST /api/admin/prompt/read` — 读取提示词

```javascript
const resp = await fetch('/api/admin/prompt/read', {
  method: 'POST', headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ token, path: '/abs/path/to/main_prompt.txt' }),
})
// data: { path, filename, content, char_count }
```

### `POST /api/admin/prompt/write` — 覆写提示词

```javascript
const resp = await fetch('/api/admin/prompt/write', {
  method: 'POST', headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ token, path: '/abs/path/to/main_prompt.txt', content: '新内容...' }),
})
```

---

## 三、会话与画像

| 操作 | 方法 | 路径 |
|---|---|---|
| 列出所有会话 | GET | `/api/sessions` |
| 获取会话历史 | GET | `/api/session/{session_id}` |
| 删除会话 | DELETE | `/api/session/{session_id}` |
| 保存画像 | POST | `/api/profile` → `{ user_id, city, gender, age, preferences }` |
| 获取画像 | GET | `/api/profile/{user_id}` |

---

## 四、反馈

| 操作 | 方法 | 路径 | 参数 |
|---|---|---|---|
| 提交反馈 | POST | `/api/feedback` | `{ session_id, rating: "like"\|"dislike", comment }` |
| 查看统计 | GET | `/api/feedback/stats` | — |

---

## 五、系统状态

| 接口 | 方法 | 说明 |
|---|---|---|
| `/health` | GET | 进程存活检查 |
| `/ready` | GET | 依赖就绪检查（LLM + Embedding + Chroma） |
| `/api/rag/status` | GET | 知识库状态（chunk 数、分类、缓存命中率） |

---

## 通用约定

**响应格式：**
```json
{ "success": true | false, "message": "说明", "data": { ... } | null }
```

**错误处理：**
```javascript
const data = await resp.json()
if (!resp.ok || !data.success) {
  throw new Error(data.message || '请求失败')
}
```
